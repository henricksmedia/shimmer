numpy>=1.26,<3
scipy>=1.11
soundfile>=0.12
mutagen>=1.47
matplotlib>=3.8
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pyloudnorm>=0.1.1

# Test dependencies (pytest suite in tests/)
pytest>=8
httpx>=0.27

# Developer tooling only (scripts/ and /static/references/). The app runs
# without it; shimmer/references.py imports it lazily and reports its absence.
# Windows WASAPI loopback capture for building the tone reference.
soundcard>=0.4.2
